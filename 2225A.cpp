#include <iostream>
#include <string>
#include <vector>
#include <algorithm>

void solve() {
    long long x, y;
    std::cin >> x >> y;
}

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    int t;
    std::cin >> t;

    while (t--) {
        solve();
    }
    return 0;
}